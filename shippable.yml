language: c

compiler: 
  - gcc
  - clang

#jdk:
 #  - openjdk6
  # - openjdk7
   #- oraclejdk7
  # - oraclejdk8
   
env:
  global: 
    - TEST=CLan
    - CLAN=BETA
  matrix:
    - a1=test
    - a2=beta
    
matrix:
  inlcude:
    - compiler: gcc
      env: test@&=beta
 # exclude:
  #  - jdk: oraclejdk7
      
build:
  pre_ci:
    - docker build -t=shippabledocker/shiphitchcock1_samplec .

  pre_ci_boot:
    image_name: drydock/u12cpp
    image_tag: prod
    pull: false
    options: '--privileged=true --net=bridge -e FOO=true -e BOO=false'

  ci:
   # - ./configure && make && make install
    #- ./hello
    - echo "$TEST"
    - echo "$CLAN"
   # - echo "$a1"
   # - echo "$a2"

  post_ci:
    #- docker build -t=shippabledocker/shiphitchcock1_testsamplenode .
    - docker push shippabledocker/shiphitchcock1_samplec
